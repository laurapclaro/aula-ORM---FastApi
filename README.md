# Instalar as bibliotecas
pip install fastapi uvicorn sqlalchemy alembic python-dotenv

# Rodar o FastApi
no terminal: python -m uvicorn main:app --reload

# Acessar documentação
acesse no navegador = http://127.0.0.1:8000/docs

# models = criar o banco
# database = criar a conexão e usa ela nos arquivos